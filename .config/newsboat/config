# auto relaod
auto-reload yes

# vim keybinds
unbind-key j
unbind-key k
unbind-key J
unbind-key K

bind-key j down
bind-key k up
bind-key l open
bind-key h quit

# open video in mpv
macro y set browser "mpv %u" ; open-in-browser ; set browser "elinks %u"
